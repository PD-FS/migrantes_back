<%= form_with(model: donation_kind, local: true) do |form| %>
  <% if donation_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donation_kind.errors.count, "error") %> prohibited this donation_kind from being saved:</h2>

      <ul>
      <% donation_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :donation_kind_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
