<%= form_with(model: donation_category, local: true) do |form| %>
  <% if donation_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donation_category.errors.count, "error") %> prohibited this donation_category from being saved:</h2>

      <ul>
      <% donation_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :donation_category_name %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon, id: :donation_category_icon %>
  </div>

  <div class="field">
    <%= form.label :donation_kind_id_id %>
    <%= form.text_field :donation_kind_id_id, id: :donation_category_donation_kind_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
